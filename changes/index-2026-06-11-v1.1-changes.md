# Changes — index.html v1.1

**Date:** 2026-06-11  ·  **Snapshot:** `versions/index-2026-06-11-v1.1.html`
**Supersedes:** v1.0 (`versions/index-2026-06-11-v1.0.html`)
**Driven by:** `audits/index-2026-06-11-audit-01.md` (all findings actioned)

> v1.1 applies every finding from Audit 01. Audit 01 statuses are now marked Done.

---

## 🔴 Must-fix (applied)
| Ref | Change |
|-----|--------|
| A1 | **Mobile nav wired.** Hamburger now opens a dropdown menu (with `aria-expanded`); links close it on tap. |
| A2 | **Dealer margin example made realistic.** New worked example on a different car (BMW 320d, 2019): £8,200 buy, −£1,150 recon, £11,400 retail = **£2,050 margin** (~25% on cost). No longer the weak 6% Golf example, and no longer collides with the moat card's figures. |
| A3 | **"Results in seconds" softened** to "Built on official UK data" in the hero trust row (AI assessment isn't instant). |
| A4 | **"Over 1,000 data points" removed**; FAQ now says "Sourced from DVLA, DVSA and OEM data." |
| A5 | **Contrast fixed.** `.valnote` moved from `--fg3` to `--fg2` (now passes WCAG AA). |
| A6 | **Focus-visible states added** on links, buttons and summaries using a stronger focus ring. |
| A7 | **Footer social icons are now real `<a>`** with `aria-label` and `rel="noopener"`, pointing at the known handles. |

## 🟡 Should-improve (applied)
| Ref | Change |
|-----|--------|
| B1 | **Report-preview visual added** ("A look inside your report"): a CSS report mockup with condition grade, key-check tiles, a valuation bar chart and the AI repair estimate. Shows the product, no external image. |
| B2 | **Dealer card differentiated** from the moat card: it now leads with a big "indicative margin" figure, then the breakdown, instead of repeating the same stacked-subtraction layout. |
| B3 | **Real trust markers added.** Trust card now carries the VAT number (385519364) and company name; secure-checkout wording made concrete. Reviews placeholder reworded ("Real reviews only"). |
| B4 | **Signup expectation set.** Hero hint and CTA microcopy now say "create a free account in seconds, no card." |
| B5 | Dealer-section placement left as-is (deferred in the audit; acceptable for serve-both). |

## 🟢 Nice-to-have (applied)
| Ref | Change |
|-----|--------|
| C1 | **Honest JSON-LD added**: `Organization` + `FAQPage` (matches the visible FAQ). **No `AggregateRating`** until reviews are real. |
| C2 | **lucide pinned** to `0.460.0` (was `@latest`). If icons ever fail to load, bump this pin to a current version. |
| C3 | **Fonts trimmed**: Onest 600/700/800, Manrope 400/500/600/700, IBM Plex Mono 500/600/700 (dropped unused weights). |
| C4 | **Favicon added** (uses the logo). OG image still a launch TODO (can't generate a 1200x630 here). |
| C5 | **`prefers-reduced-motion`** media query added; disables transitions/hover transforms. |
| C6 | **Heading hierarchy fixed**: feature, step and trust-card titles changed from `h4` to `h3` under their section `h2`. |

## Still open / out of scope for v1.1
- **OG share image** (1200x630): asset to be created before launch.
- **Real backend wiring**: reg/VIN form, "Get my first report free", "Buy credits" still link to `#`.
- **Provenance block** stays disabled pending confirmation of finance/write-off/stolen delivery.
- **Trade-first hero variant** (strategic A/B) not built; available on request.
- **Visual QA** at desktop + mobile widths recommended (this round was code-level).

## Files in this version
- `versions/index-2026-06-11-v1.1.html` — frozen snapshot
- `changes/index-2026-06-11-v1.1-changes.md` — this file
- `index.html` — live working copy (identical to v1.1)
